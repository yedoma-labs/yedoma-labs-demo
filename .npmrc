# pnpm configuration
strict-peer-dependencies=false
auto-install-peers=true
shamefully-hoist=false

# Performance
prefer-frozen-lockfile=true

# Disable minimum release age check — demo project, we own the packages
minimum-release-age=0
